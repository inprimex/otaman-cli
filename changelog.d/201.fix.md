Installing otaman-cli from a wheel now carries the NOTICE file alongside the
licence. Attribution and the SPDX identifier moved into NOTICE when the licence
files were split, but the packaging still declared only LICENSE, so a `pip
install` no longer delivered them.
