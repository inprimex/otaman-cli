Agent identity is now resolved by one shared implementation across the toolchain,
so a session in a git worktree is recognised as belonging to its repo instead of
falling back to whatever the environment last claimed. An owner name written with
stray spaces in platform.yaml is also normalised rather than carried through.
