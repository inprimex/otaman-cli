The lifecycle report no longer invents approvals that never happened, and
`otaman blocked --migrate` no longer clears a blocked entry on the strength of a
message that approved nothing. Both were matching a message's type by its
filename; a message whose subject merely mentioned an approval counted as one.
Type now comes from the message itself.
