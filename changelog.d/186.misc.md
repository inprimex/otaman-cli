Bus message filenames are now built in one place instead of nineteen. The
convention is unchanged, so existing messages and tooling are unaffected, but
subjects that previously slugged to nothing — or whose slug was truncated onto
a trailing hyphen — now produce a clean filename rather than one ending in a
stray dash.
