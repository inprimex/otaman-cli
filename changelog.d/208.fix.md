`otaman complete` now refuses a change name that does not exist, instead of
reporting "task-complete sent" for work nothing will ever tick. A typo is told
what it probably meant. When the specs repo is not checked out the name cannot
be verified, and the command says so rather than passing in silence.
