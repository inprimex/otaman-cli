`otaman blocked --list` no longer omits malformed entries. An entry whose title
was empty or missing did not appear in the list at all, so a file with three
blocked items showed two with nothing to indicate the third existed. Such
entries are now listed as `[malformed]`, matching what every other view of the
same file already showed.
