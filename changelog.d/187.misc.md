Bus message frontmatter is now read by one shared parser, so a `cc:` written as
a single name and an `x-cc` left as text both read the same way whichever tool
wrote the message. Path-ownership patterns also follow the ruled semantics: a
bare directory name in `owner-paths` now owns everything beneath it, where
before it owned only the directory entry itself.
