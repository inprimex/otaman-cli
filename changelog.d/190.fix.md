`otaman send` now refuses an address that names several agents, instead of
reporting success for a message nobody receives. Addressing a message to
"agent-a, agent-b" produced a file neither agent's inbox matched, while the
sender was told it had been sent. Use `--cc` to reach more than one agent — the
refusal says so and shows the command.
