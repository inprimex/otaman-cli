Clearing a blocked entry now removes exactly that entry's own text, leaving
every other entry in the file untouched byte-for-byte — including ones already
cleared and ones with a missing title. Matching a blocked entry by its proposal
reference requires the full reference rather than a fragment, so an unrelated
entry cannot be cleared by a partial match.
