`otaman knowledge list` with a filter that matches nothing now says so, instead
of reporting that no entries have been recorded at all. Filtering five entries
down to none told you the knowledge base was empty and offered to help you add
your first entry — both misleading. It now names the filter that matched nothing
and how many entries exist.
