# {{ program_name }} — strategy

Strategy artifacts for the **{{ program_name }}** program.

Sibling to `{{ program_slug }}-specs`. Managed by the `cofounder-agent`.

Initial contents:

- `program-meta.yaml` — program identity stub

Over time:

- Pitch deck, business plan, GTM, financials, market analyses

See `CLAUDE.md` for the agent ownership + workflow contract.
