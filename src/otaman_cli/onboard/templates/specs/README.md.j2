# {{ program_name }} — specs

Technical specifications for the **{{ program_name }}** program.

Sibling to `{{ program_slug }}-otaman/` (meta) and the program's code repos.
Managed by the `spec-agent`.

Initial contents:

- (empty — populated by spec-agent as proposals land)

When OpenSpec is enabled (see `openspec/`):

- `openspec/specs/<capability>/spec.md` — capability specs
- `openspec/changes/<change-name>/` — change proposals with tasks.md
- ADRs and design docs as they're authored

See `CLAUDE.md` for the agent ownership + workflow contract.
