"""`otaman init` launcher-scaffold module (otaman-init-dev-scaffold spec).

Submodules:
    schema     — Pydantic models for launch-settings.yaml + load_settings()
    wizard     — Interactive prompts that build a LaunchSettings
    generator  — Renders launch-settings.yaml + launch.sh/ps1 + .gitignore
    templates/ — Jinja2 templates for launch.sh + launch.ps1
"""
